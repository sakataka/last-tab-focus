# Chrome Web Store Descriptions

The six sections below are the source of truth for the localized long descriptions in the Chrome Web Store. Korean (`ko`) is a future candidate, not part of version 1.1.9.

## en

Close the current Chrome tab and return to the tab you were using immediately before it—not simply the tab to the left or right.

Last Tab Focus changes one specific behavior. When you close the current tab with Ctrl+W or Command+W, it focuses the previously active tab in the same Chrome window.

For example, if tabs A, B, and C are open, you use A and then move to C, closing C returns you to A. It does not choose B just because B is next to C. If you use A, then B, then C, closing C returns to B; closing B then returns to A.

This extension does not reopen closed tabs. It changes which still-open tab receives focus after the current tab closes. It is also not a general tab manager: there are no tab groups, side panels, saved sessions, workspaces, or sync features.

Everything stays local to Chrome:

- Works within the same browser window
- Keeps only session-scoped tab focus history
- No analytics or tracking
- No external communication or remote services
- No browsing data is sent anywhere

One small extension, one predictable action: close the current tab and go back to the last tab you actually used.

## ja

Chromeで今のタブを閉じると、右隣や左隣ではなく、直前まで見ていたタブへ自動的に戻ります。閉じたタブを復元する拡張機能ではありません。

Last Tab Focus が変えるのは、タブを閉じたあとの戻り先だけです。Ctrl+W または Command+W で現在のタブを閉じると、同じ Chrome ウィンドウ内で直前まで使っていた、まだ開いているタブにフォーカスします。

たとえばタブ A、B、C が並んでいて、A を使ったあと C に移動した場合、C を閉じると隣の B ではなく A に戻ります。A → B → C の順で使った場合は、C を閉じると B、続けて B を閉じると A に戻ります。

閉じたタブを再度開く機能ではなく、現在のタブを閉じたあとに、どの開いているタブを表示するかを変える機能です。一般的なタブ管理ツールでもありません。タブグループ、サイドパネル、セッション保存、ワークスペース、同期機能はありません。

すべてChrome内のローカル処理で完結します。

- 同じブラウザウィンドウ内だけで動作
- タブの利用履歴はブラウザセッション中だけ保持
- アナリティクスやトラッキングなし
- 外部通信やリモートサービスなし
- 閲覧データの送信なし

今のタブを閉じて、実際に直前まで使っていたタブへ戻る。その一つの動作だけを行う小さな拡張機能です。

## zh_CN

关闭当前 Chrome 标签页后，回到刚才使用的标签页，而不是简单地切换到左侧或右侧的相邻标签页。本扩展不会重新打开已关闭的标签页。

Last Tab Focus 只改变一个行为：使用 Ctrl+W 或 Command+W 关闭当前标签页时，它会在同一个 Chrome 窗口中聚焦最后一个处于活动状态、并且仍然打开的标签页。

例如，标签页 A、B、C 并排打开。先使用 A，再切换到 C，关闭 C 后会回到 A，而不是因为 B 与 C 相邻就选择 B。若按 A → B → C 的顺序使用，关闭 C 会回到 B，继续关闭 B 则会回到 A。

它不会恢复或重新打开已关闭的标签页，只会决定关闭当前标签页后聚焦哪个仍然打开的标签页。它也不是通用标签页管理器：没有标签页分组、侧边栏、会话保存、工作区或同步功能。

所有处理都在 Chrome 本地完成：

- 仅在同一个浏览器窗口内工作
- 只在当前浏览器会话中保留标签页焦点历史
- 无分析统计或跟踪
- 无外部通信或远程服务
- 不会发送浏览数据

一个小扩展，只做一件可预测的事：关闭当前标签页，回到你刚刚真正使用的标签页。

## zh_TW

關閉目前的 Chrome 分頁後，回到剛才使用的分頁，而不是單純切換到左側或右側的相鄰分頁。本擴充功能不會重新開啟已關閉的分頁。

Last Tab Focus 只改變一個行為：使用 Ctrl+W 或 Command+W 關閉目前分頁時，它會在同一個 Chrome 視窗中聚焦最後一個使用過、而且仍然開啟的分頁。

例如分頁 A、B、C 並排開啟。先使用 A，再切換到 C，關閉 C 後會回到 A，而不是因為 B 與 C 相鄰就選擇 B。若依 A → B → C 的順序使用，關閉 C 會回到 B，接著關閉 B 則會回到 A。

它不會還原或重新開啟已關閉的分頁，只會決定關閉目前分頁後聚焦哪一個仍然開啟的分頁。它也不是一般的分頁管理工具：沒有分頁群組、側邊欄、工作階段保存、工作區或同步功能。

所有處理都在 Chrome 本機完成：

- 只在同一個瀏覽器視窗內運作
- 只在目前瀏覽器工作階段保留分頁焦點記錄
- 沒有分析或追蹤
- 沒有對外通訊或遠端服務
- 不會傳送瀏覽資料

一個小型擴充功能，只做一件可預期的事：關閉目前分頁，回到你剛才真正使用的分頁。

## ru

Закройте текущую вкладку Chrome и вернитесь к вкладке, которой вы пользовались непосредственно перед ней, а не просто к соседней вкладке слева или справа. Расширение не восстанавливает закрытые вкладки.

Last Tab Focus меняет только одно действие. Когда вы закрываете текущую вкладку сочетанием Ctrl+W или Command+W, расширение переводит фокус на последнюю активную вкладку в том же окне Chrome, если она всё ещё открыта.

Например, открыты вкладки A, B и C. Если сначала использовать A, затем перейти на C и закрыть C, откроется A, а не соседняя B. При работе в порядке A → B → C закрытие C вернёт вас к B, а последующее закрытие B — к A.

Расширение не открывает заново закрытую вкладку: оно лишь выбирает, какая из уже открытых вкладок получит фокус. Это также не универсальный менеджер вкладок: здесь нет групп, боковой панели, сохранения сеансов, рабочих пространств или синхронизации.

Вся обработка выполняется локально в Chrome:

- Работает только в пределах одного окна браузера
- Хранит историю фокуса только в течение текущего сеанса
- Без аналитики и отслеживания
- Без внешних подключений и удалённых сервисов
- Данные о просмотре никуда не отправляются

Небольшое расширение с одним предсказуемым действием: закрыть текущую вкладку и вернуться к последней вкладке, которой вы действительно пользовались.

## it

Chiudi la scheda Chrome corrente e torna alla scheda che stavi usando subito prima, non semplicemente a quella adiacente a sinistra o a destra. L'estensione non riapre le schede chiuse.

Last Tab Focus modifica un solo comportamento. Quando chiudi la scheda corrente con Ctrl+W o Command+W, mette in primo piano l'ultima scheda attiva ancora aperta nella stessa finestra di Chrome.

Per esempio, con le schede A, B e C aperte, se usi A e poi passi a C, chiudendo C torni ad A, non alla scheda B solo perché è adiacente. Se usi le schede nell'ordine A → B → C, chiudendo C torni a B e chiudendo poi B torni ad A.

L'estensione non ripristina né riapre la scheda chiusa: sceglie soltanto quale scheda ancora aperta riceve il focus. Non è nemmeno un gestore di schede generico: non include gruppi, pannelli laterali, salvataggio delle sessioni, spazi di lavoro o sincronizzazione.

Tutto avviene localmente in Chrome:

- Funziona solo all'interno della stessa finestra del browser
- Conserva la cronologia del focus solo per la sessione corrente
- Nessuna analisi e nessun tracciamento
- Nessuna comunicazione esterna o servizio remoto
- Nessun dato di navigazione viene inviato

Una piccola estensione, una sola azione prevedibile: chiudi la scheda corrente e torna all'ultima scheda che hai effettivamente utilizzato.
